program main
  use matrices, only : print_mat, I2, I4, s1, s2, s3, a1, a2, a3, g0
  implicit none
  

call print_mat(I2)
call print_mat(I4)
call print_mat(s1)
call print_mat(s2)
call print_mat(s3)
call print_mat(a1)
call print_mat(a2)
call print_mat(a3)
call print_mat(g0)
end program main
